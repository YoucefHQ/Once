version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
    volumes:
      - ./:/opt/app
      - node_modules:/opt/app/node_modules

volumes:
  node_modules:
